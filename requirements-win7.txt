# Windows 7 兼容的依赖版本
# Python 3.8.10 是最后一个支持 Win7 的 Python 版本

# PDF处理
pdfplumber==0.6.2
pdfminer.six==20211012
PyPDF2==2.12.1

# 字符编码
charset-normalizer==2.0.12
chardet==4.0.0

# 打包工具（Win7兼容版本）
PyInstaller==4.10

# 其他依赖
Pillow==8.4.0
Wand==0.6.7
